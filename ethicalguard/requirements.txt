# EthicalGuard — Python dependencies
#
# On Colab: install only what's missing, let Colab's environment handle
# the rest. Do NOT use exact pins (==) on Colab — they conflict with
# pre-installed packages like gradio, google-adk, langchain.
#
# Install command for Colab:
#   pip install -r requirements.txt -q
#   pip install groq -q

# --- API framework ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# --- Core ML ---
torch>=2.0.0
transformers>=4.40.0        # Phi-3 requires >=4.40; kept for tokenizers/pipelines
accelerate>=0.30.0
sentence-transformers>=2.7.0

# --- Generation API ---
groq                        # Groq API client for Llama-3.3-70B generation

# --- Vector DB ---
faiss-cpu

# --- Document ingestion ---
pdfplumber>=0.11.0
PyPDF2>=3.0.1

# --- Data / validation ---
pydantic>=2.7.0
numpy
requests>=2.32.0
