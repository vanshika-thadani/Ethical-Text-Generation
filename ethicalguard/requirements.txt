# EthicalGuard — Python dependencies
#
# RAM profile (torch-free):
#   Python + FastAPI overhead: ~50MB
#   No torch / transformers / sentence-transformers loaded
#   All ML inference via external APIs (Groq + HF Inference)
#   Total: ~50MB ✅ fits Render free tier (512MB) with ease
#
# Required environment variables:
#   GROQ_API_KEY  → https://console.groq.com  (free)
#   HF_API_KEY    → https://huggingface.co/settings/tokens  (free)

# --- API framework ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# --- API clients ---
groq                   # Groq API for Llama-3.3-70B generation
requests>=2.32.0       # HF Inference API HTTP calls

# --- Vector DB (CPU-only numpy index, no torch needed) ---
faiss-cpu
numpy

# --- Document ingestion ---
pdfplumber>=0.11.0
PyPDF2>=3.0.1

# --- Data / validation ---
pydantic>=2.7.0
