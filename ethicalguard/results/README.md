# Results

This directory is populated by `evaluate.py`.

After running the benchmark:
- `evaluation_results.csv` — per-prompt scores for baseline and safety-ranked outputs
- `summary.json` — aggregate averages across all prompts and categories
